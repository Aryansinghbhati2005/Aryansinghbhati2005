# Data Directory

Generated demo datasets are written here by `python -m src.fraud_detection.data` or by the training pipeline. CSV files are ignored by git to avoid committing bulky or sensitive transaction data.
