# Models Directory

Training creates model artifacts in this folder:

- `fraud_classifier.joblib`
- `anomaly_detector.joblib`
- `metrics.json`

These outputs are ignored by git so models can be regenerated locally or in CI.
